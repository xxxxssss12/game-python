/**
 * Created by xs on 2017/7/24.
 */
function DirectionChange(beforeDirection, afterDirection, x, y) {
    this.beforeDirection = beforeDirection;
    this.afterDirection = afterDirection;
    this.x = x;
    this.y = y;
}